[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"
watchPaths = ["services/polymarket-worker/**"]

[deploy]
numReplicas = 1
sleepApplication = false
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
