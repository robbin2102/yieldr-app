[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"
watchPaths = ["services/monitoring-scheduler/**"]

[deploy]
startCommand = "node dist/index.js"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3

[[services]]
name = "monitoring-scheduler"
