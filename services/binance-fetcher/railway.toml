[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"
watchPaths = ["services/binance-fetcher/**"]

[deploy]
startCommand = "node dist/index.js"
healthcheckPath = "/health"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
